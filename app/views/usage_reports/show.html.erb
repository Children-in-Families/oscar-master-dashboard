<% content_for :page_title do %>
  <%= "Usage Report for #{params[:q][:month_eq]}/#{params[:q][:year_eq]}" %>
<% end %>

<%= render "tabs_menu" %>
<%= render "usage_reports/content/#{params[:id]}" %>
